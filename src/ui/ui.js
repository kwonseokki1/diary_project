import styled from "styled-components";

export const Button = styled.button`
  width: ${(props) => `${props.size}px`};
  height: 60px;
  background: #0174df;
  border: none;
  border-radius: 10px;
`;
